services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: folio
      POSTGRES_USER: folio
      POSTGRES_PASSWORD: folio
    ports:
      - "5432:5432"
    volumes:
      - folio_postgres:/var/lib/postgresql/data
volumes:
  folio_postgres:
